# Database - Required for the app to run
# Note: This is a placeholder. You'll need to replace with your actual database URL
DATABASE_URL="postgresql://user:password@localhost:5432/animal_lab_db"

# Server Configuration
PORT=5000
NODE_ENV=development
HOST=0.0.0.0

# Session Secret - Required for authentication
SESSION_SECRET="dev-secret-change-in-production"

# Optional: Email service (Resend)
# RESEND_API_KEY="your_resend_api_key_here"

# Optional: Google Cloud Storage (for file uploads)
# GCP_STORAGE_BUCKET="your_bucket_name"
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/credentials.json"

# Replit Auth (if using Replit authentication)
# REPL_ID="your_repl_id"
# REPL_SLUG="your_repl_slug"
